#include <stdio.h>
#include <stdlib.h>

int main()
{
      int a;
      printf("entrer le nomber :");scanf("%d",&a);
      if(a<0) printf("le nomber est negative ");
      else if (a==0) printf("le nomber  egal zero ");
      else printf("le nomber est positive");


    return 0;
}
