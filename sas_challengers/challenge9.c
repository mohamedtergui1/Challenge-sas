#include <stdio.h>
#include <stdlib.h>

int main()
{
        int n;
        printf("entret une nomber entiee:");scanf("%d",&n);
        if (n%2==0)printf("le nomber est pair");
        else printf("le nomber est impair");
    return 0;
}
